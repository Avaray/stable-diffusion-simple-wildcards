name: checking how it works

on:
  workflow_dispatch:

jobs:
  get-repo-name:
    name: get-repo-name
    runs-on: ubuntu-latest
    steps:
      - name: Get repository name
        id: repo-name
        run: echo "REPOSITORY_NAME=${GITHUB_REPOSITORY#*/}" >> $GITHUB_ENV
